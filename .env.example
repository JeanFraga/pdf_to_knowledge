# Environment Variables for Local Development
# Copy to .env and fill in values

# GCP Project (REQUIRED for setup.sh and cloud integrations)
GCP_PROJECT_ID=pdf-to-knowledge
GCP_REGION=us-central1

# Google AI / Gemini API Key (REQUIRED for local development)
# Get one at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Agent Model Configuration (per-agent, allows tuning based on task complexity)
# Options: gemini-2.0-flash (fast), gemini-2.5-flash (balanced), gemini-2.0-pro (complex)
INGESTION_AGENT_MODEL=gemini-2.0-flash
DATABASE_AGENT_MODEL=gemini-2.0-flash

# Service Account (for non-ADC environments)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Agent Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO
